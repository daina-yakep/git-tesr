import React from 'react';
import './styles/Dashboard.css';

const Dashboard = () => {
  const stats = {
    totalBooks: 120,
    borrowedBooks: 45,
    users: 30,
    lateReturns: 5
  };

  const recentBorrows = [
    { user: 'Alice Dupont', book: 'Le Petit Prince', date: '2025-04-20', due: '2025-05-04' },
    { user: 'Jean Martin', book: 'L\'Étranger', date: '2025-04-18', due: '2025-05-02' },
  ];

  return (
    <div className="dashboard-container">
      <h1>📚 Tableau de bord - Bibliothèque</h1>

      <div className="stats-grid">
        <div className="card">📘 Livres totaux : <strong>{stats.totalBooks}</strong></div>
        <div className="card">📕 Empruntés : <strong>{stats.borrowedBooks}</strong></div>
        <div className="card">👥 Utilisateurs : <strong>{stats.users}</strong></div>
        <div className="card">⏰ Retards : <strong>{stats.lateReturns}</strong></div>
      </div>

      <div className="recent-section">
        <h2>📖 Emprunts récents</h2>
        <table className="recent-table">
          <thead>
            <tr>
              <th>Utilisateur</th>
              <th>Livre</th>
              <th>Date emprunt</th>
              <th>Date retour</th>
            </tr>
          </thead>
          <tbody>
            {recentBorrows.map((item, idx) => (
              <tr key={idx}>
                <td>{item.user}</td>
                <td>{item.book}</td>
                <td>{item.date}</td>
                <td>{item.due}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
};

export default Dashboard;
